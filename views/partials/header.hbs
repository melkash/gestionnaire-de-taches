<header>
  <nav class="navbar navbar-expand-lg navbar-light bg-light">
    <div class="container">
      <a class="navbar-brand" href="/">Mon Gestionnaire</a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav me-auto"> 
           <!-- Liens visibles uniquement pour les utilisateurs connectés -->
          {{#if user}}
           {{#unless (eq currentPath '/exercises')}}
            <li class="nav-item">
              <a class="nav-link" href="/exercises">Liste des exercices</a>
            </li>
            {{/unless}}
            {{/if}}
        </ul>
        <ul class="navbar-nav ms-auto">
          {{#if user}}
            <li class="nav-item">
              <a class="nav-link" href="/auth/logout">Se déconnecter</a>
            </li>
          {{else}}
            <!-- Liens visibles uniquement pour les utilisateurs non connectés -->
            {{#if (eq currentPath '/auth/login')}}
              <!-- Ne rien afficher -->
            {{else if (eq currentPath '/auth/register')}}
              <!-- Ne rien afficher -->
            {{else}}
              <li class="nav-item">
                <a class="nav-link" href="/auth/login">Se connecter</a>
              </li>
              <li class="nav-item">
                <a class="nav-link" href="/auth/register">S'inscrire</a>
              </li>
            {{/if}}
          {{/if}}
        </ul>
      </div>
    </div>
  </nav>
</header>


